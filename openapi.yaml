# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Commission API
    version: 0.0.1
paths:
    /v1/total_commissions:
        get:
            tags:
                - Commission
            description: 列出每个用户的累计佣金
            operationId: Commission_ListTotalCommission
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListTotalCommissionReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/users/{parentId}/children/total_commissions:
        get:
            tags:
                - Commission
            description: 列出每个下游代理的累计佣金
            operationId: Commission_ListTotalCommissionByParent
            parameters:
                - name: parentId
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ListTotalCommissionByParentReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /v1/users/{userId}/total_commission:
        get:
            tags:
                - Commission
            description: 获取指定用户的累计佣金
            operationId: Commission_GetUserTotalCommission
            parameters:
                - name: userId
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetUserTotalCommissionReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        GetUserTotalCommissionReply:
            type: object
            properties:
                id:
                    type: string
                userId:
                    type: string
                totalCommission:
                    type: integer
                    format: int32
                todayCommission:
                    type: integer
                    format: int32
                settledCommission:
                    type: integer
                    format: int32
                totalRegistrationCount:
                    type: integer
                    format: int32
                todayRegistrationCount:
                    type: integer
                    format: int32
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        ListTotalCommissionByParentReply:
            type: object
            properties:
                commissions:
                    type: array
                    items:
                        $ref: '#/components/schemas/GetUserTotalCommissionReply'
        ListTotalCommissionReply:
            type: object
            properties:
                commissions:
                    type: array
                    items:
                        $ref: '#/components/schemas/GetUserTotalCommissionReply'
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
tags:
    - name: Commission
